# ML inference & training
# NOTE: torch/torchvision/torchaudio are installed in the Dockerfile
# directly from https://download.pytorch.org/whl/cu121 for reliability.
# Do NOT add them back here.
ultralytics==8.3.52
numpy==1.26.4
opencv-python-headless==4.10.0.84
Pillow==11.0.0
scikit-learn==1.5.2
matplotlib==3.9.3
seaborn==0.13.2
tensorrt==10.6.0; sys_platform == "linux"  # TRT only on Linux/Docker
pycuda==2024.1; sys_platform == "linux"
